{
  "name": "murchat-monorepo",
  "version": "0.0.94",
  "scripts": {
    "server-start": "cd server && npm start",
    "client-dev": "cd murchat && npm run dev",
    "dev": "npm run server-start & npm run client-dev",
    "dist": "cd murchat && npm install && npm run dist"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0",
    "uuid": "^13.0.0"
  }
}
